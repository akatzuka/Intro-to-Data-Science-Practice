v1 = c("foo", "bar", "baz")
z = v1[2]
v2 = (1:3)
v3 = v1
v3 = paste0(v1, v2)
x = sample(1:500, 10)
avg_x = mean(x)
m_avg_x = (sum(x) / length(x))

# Q8.mean takes the average of the provided value. trim truncates the given value to a provided width. default trim on the mean function is 0.
q_9 = mean(x, 0.2)
one_to_fhun = (1:500)
q_10 = mean(sample(one_to_fhun, 10))
# Q11. Min = 194 Max =280.8
#q_12 = sample(one_to_fhun, )